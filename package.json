{
  "name": "novex-holiday-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install --prefix client && npm install --prefix server"
  }
}
